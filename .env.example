# Copy to .env and adjust if needed.
# Port 5433 = the docker-compose PostgreSQL (host 5432 is the local EDB Postgres).
EUDAMED_DATABASE_URL=postgresql+psycopg://eudamed:eudamed@localhost:5433/eudamed
